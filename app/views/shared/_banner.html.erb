<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%= image_path 'enterrement.jpg' %>');">
  <div id="songs">
  <%= audio_tag("sound.mp3", autoplay: true, controls: true) %>
  </div>
  <div class="banner-content">
    <h1>Cry me a river</h1>
      <p>
      <span id="banner-typed-text"></span>
      </p>
      <% if policy(Event).new? %>
        <%= link_to new_event_path, class:"btn btn-primary btn-lg" do %>
        Create an event !
      <% end %>
      <% end %>
  </div>
</div>
